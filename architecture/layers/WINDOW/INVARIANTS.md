# WINDOW — Invariants

These invariants are absolute.
Violation triggers fail-closed posture.

- WINDOW is public.
- WINDOW does NOT execute.
- WINDOW does NOT decide.
- WINDOW does NOT infer intent.
- WINDOW does NOT expose internal systems.

Presentation without authority is mandatory.
